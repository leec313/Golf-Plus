{% extends "base.html" %}
{% block content %}

<h2>Post Detail</h2>
<ul>
    {% for postdetail in postdetails %}
    <li>{{ post.title }}</li>
    <li>{{ post.author }}</li>
    <li>{{ post.featured_image }}</li>
    <li>{{ post.created_on }}</li>
    <li>{{ post.content }}</li>
    <li>{{ post.likes }}</li>
    {% endfor %}
</ul>

{% endblock %}